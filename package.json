{
  "name": "blog-server",
  "version": "1.0.0",
  "description": "this is server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "halo",
  "license": "ISC",
  "dependencies": {
    "koa-router": "^9.4.0"
  }
}
